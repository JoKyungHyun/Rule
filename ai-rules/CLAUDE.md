# CLAUDE.md

AI 에이전트 행동 지침 + 프로젝트 규칙 통합 파일.

---

## 1. Think Before Coding

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:
- 가정을 명시적으로 밝힌다. 불확실하면 질문한다.
- 해석이 여러 가지라면 제시한다 — 조용히 선택하지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반박한다.
- 불명확한 게 있으면 멈춘다. 무엇이 모호한지 명시하고 질문한다.

## 2. Simplicity First

**문제를 해결하는 최소한의 코드. 추측성 코드는 없다.**

- 요청받지 않은 기능은 추가하지 않는다.
- 단일 사용 코드에 추상화를 넣지 않는다.
- 요청하지 않은 유연성·설정 가능성을 추가하지 않는다.
- 200줄로 썼는데 50줄로 가능하면 다시 쓴다.

스스로 확인: "시니어 엔지니어가 과도하게 복잡하다고 할까?" → 그렇다면 단순화한다.

## 3. Surgical Changes

**반드시 필요한 것만 건드린다. 자신이 만든 것만 정리한다.**

기존 코드 수정 시:
- 인접한 코드·주석·포매팅을 "개선"하지 않는다.
- 문제없는 코드를 리팩토링하지 않는다.
- 기존 스타일이 다르더라도 맞춘다.
- 관련 없는 dead code를 발견하면 언급만 한다 — 삭제하지 않는다.

자신의 변경으로 생긴 orphan은 정리:
- 자신의 변경으로 인해 미사용이 된 import/변수/함수는 제거한다.
- 기존의 dead code는 요청받지 않는 한 제거하지 않는다.

기준: 변경된 모든 줄이 사용자의 요청으로 직접 추적 가능해야 한다.

## 4. Goal-Driven Execution

**성공 기준을 정의한다. 검증될 때까지 루프한다.**

작업을 검증 가능한 목표로 변환한다:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트 작성 후 통과시킨다"
- "버그 수정" → "재현 테스트 작성 후 통과시킨다"
- "리팩토링" → "전후로 테스트가 통과함을 확인한다"

다단계 작업은 간략한 계획을 먼저 제시한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

---

## 프로젝트 규칙 참조

아래 파일들을 작업 전 반드시 확인한다.

@AGENTS.md      <!-- 에이전트 역할, 작업 흐름, 브랜치 전략 -->
@coding.md      <!-- 코딩 컨벤션, 타입, 테스트, 린터 -->
@global-learnings.md  <!-- API 설계, DB, 성능, 환경 설정 -->
@security.md    <!-- 입력 검증, 인증, 개인정보, API 보안 -->

---

**이 지침들이 잘 동작하고 있다면:** diff에 불필요한 변경이 줄고, 과도한 구현으로 인한 재작성이 줄며, 실수 후가 아닌 구현 전에 질문이 나온다.
